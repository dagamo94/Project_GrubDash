const router = require("express").Router();

// TODO: Implement the /orders routes needed to make the tests pass

// router for '/' (GET(LIST), POST)


// router for '/' (GET(READ), POST, PUT, DELETE)

module.exports = router;
